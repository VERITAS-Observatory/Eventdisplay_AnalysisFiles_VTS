- Added XGB analysis support across the IRF assets: new `DispXGBs/` copy script, new `ANASUM.XGB.runparameter`, and new XGB gamma/hadron cut files for stereo and preselection workflows.
- Generalized the model-copy tooling so `copy_dispBDTs.sh` and `copy_dispXGB.sh` now take simulation type, epochs, and atmosphere as inputs instead of relying on hardcoded settings.
- Updated IRF packaging and validation scripts to include XGB products and the newer effective-area naming/layout, including transfer tarball creation for XGB models.
- Refreshed configuration metadata for the current release: citation release date, one epoch boundary fix in `VERITAS.Epochs.runparameter`, and small cleanup in existing runparameter files.
