- Sync V6 download manifest (`transfer/file_list_V6.dat`) with epoch definitions: add IRF tarballs for `V6_2024_2024s`, `V6_2024_2025w`, `V6_2025_2025s`, and `V6_2025_2026w` across all file categories (DispBDTs, EffectiveAreas, GammaHadronBDTs, Tables).
- `download_irfs.sh`: skip extraction and cleanup when transfer fails (F8).
- `DispBDTs/copy_dispBDTs.sh`: comment out stale SIMTYPE assignments so only the active value is used; fix `${IRVERSION}` → `${IRFVERSION}` in log message (F9).
- `DispXGB/copy_dispXGB.sh`: comment out stale SIMTYPE assignments (F9).
- `GammaHadronBDTs/copy_GammaHadron_V4_V5_BDTs.sh`: fix dead atmosphere condition `ATM62` → `ATM22` so the summer branch is actually reachable for V4/V5 epochs (F10).
- `README.md`: correct stale `EVNDISP.reconstruction.runparameter` reference, fix maintainer step 2 directory name, and add note on XGB file availability and limited coverage (F3, F5, F7).
